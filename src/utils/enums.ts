export enum EventTypes {
    LOCAL = 'local',
    EXTERNAL = 'external',
    EMBEDDED = 'embedded', // old on-demand
    ON_DEMAND = 'on-demand',
    TELEVEDA_BINGO = 'televeda-bingo',
    IN_PERSON = 'in-person',
    WEBEX = 'webex',
    VTC = 'vtc'
  }
  